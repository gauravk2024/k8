apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: discovery-health-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloud-api-gateway
            port:
              number: 80 